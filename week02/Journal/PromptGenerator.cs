using System;

class PromptGenerator
{
    public List<string>_prompts = new List<string>();

    public string GetRandomPrompt()
        Console.Write(entry._name = "Who was the most important person I interacted with today? ");
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "What was the best part of my day? ");
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "How did I see the hand of the Lord in my life today? ");
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "What was the strongest emotion I felt today? ");
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "If I had one thing I could do over today, what would it be? ");
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "What are you going to do differently tomorrow? ");
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "What is somthing you wish you did today? ");  
        entry._entries.Add(new Entry());
        Console.Write(entry._name = "What did you learn about yourself today? ");
        entry._entries.Add(new Entry());
    
}